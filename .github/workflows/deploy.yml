name: Deploy to AWS

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Install SAM CLI
        uses: aws-actions/setup-sam@v2

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Verify AWS credentials
        run: |
          echo "Verificando credenciais AWS..."
          aws sts get-caller-identity

      # PASSO 1: Usar o bucket específico da política
      - name: Set bucket name
        id: bucket
        run: |
          # Bucket específico da política IAM
          BUCKET_NAME="daily-news-deploy-bucket-891612551514"
          echo "Bucket name: $BUCKET_NAME"
          
          # Verificar se o bucket existe, se não, criar
          if ! aws s3 ls "s3://$BUCKET_NAME" 2>&1 > /dev/null; then
            echo "Bucket não existe. Criando..."
            aws s3 mb s3://$BUCKET_NAME --region us-east-1
          else
            echo "Bucket já existe"
          fi
          
          echo "bucket_name=$BUCKET_NAME" >> $GITHUB_OUTPUT

      # PASSO 2: Limpar stack problemático do SAM
      - name: Clean up SAM managed stack
        continue-on-error: true
        run: |
          echo "Verificando stack aws-sam-cli-managed-default..."
          if aws cloudformation describe-stacks --stack-name aws-sam-cli-managed-default 2>/dev/null; then
            echo "Stack encontrado. Deletando..."
            aws cloudformation delete-stack --stack-name aws-sam-cli-managed-default
            aws cloudformation wait stack-delete-complete --stack-name aws-sam-cli-managed-default
          fi

      - name: Validate SAM template
        run: |
          echo "Validando template SAM..."
          sam validate --template iac_template.yaml

      - name: Build SAM application
        run: |
          echo "Iniciando build da aplicação..."
          sam build --template iac_template.yaml

      - name: Package SAM application
        run: |
          echo "Empacotando aplicação no bucket: ${{ steps.bucket.outputs.bucket_name }}"
          
          sam package \
            --template-file .aws-sam/build/template.yaml \
            --output-template-file packaged.yaml \
            --s3-bucket ${{ steps.bucket.outputs.bucket_name }} \
            --s3-prefix "daily-news-prod"

      - name: Deploy SAM application
        id: deploy
        run: |
          echo "Iniciando deploy..."
          
          sam deploy \
            --template-file packaged.yaml \
            --stack-name daily-news-service-prod \
            --capabilities CAPABILITY_IAM CAPABILITY_AUTO_EXPAND \
            --parameter-overrides Environment=prod \
            --s3-bucket ${{ steps.bucket.outputs.bucket_name }} \
            --s3-prefix "daily-news-prod" \
            --no-confirm-changeset \
            --no-fail-on-empty-changeset \
            --region us-east-1 \
            2>&1 | tee deploy.log
          
          if [ ${PIPESTATUS[0]} -eq 0 ]; then
            echo "✅ Deploy concluído com sucesso"
            echo "deploy_success=true" >> $GITHUB_OUTPUT
          else
            echo "❌ Deploy falhou"
            echo "deploy_success=false" >> $GITHUB_OUTPUT
            exit 1
          fi

      - name: Get stack outputs
        if: steps.deploy.outputs.deploy_success == 'true'
        run: |
          echo "Outputs do stack:"
          aws cloudformation describe-stacks \
            --stack-name daily-news-service-prod \
            --query 'Stacks[0].Outputs' \
            --output table

      - name: Diagnose on failure
        if: failure()
        run: |
          echo "========================================="
          echo "🔍 DIAGNÓSTICO DE ERRO"
          echo "========================================="
          
          # Verificar bucket
          echo "1. Status do bucket:"
          aws s3 ls s3://${{ steps.bucket.outputs.bucket_name }} || echo "Bucket não acessível"
          
          # Verificar eventos do CloudFormation
          echo "2. Eventos do CloudFormation:"
          aws cloudformation describe-stack-events \
            --stack-name daily-news-service-prod \
            --query 'StackEvents[?ResourceStatus==`CREATE_FAILED`].[LogicalResourceId,ResourceStatusReason]' \
            --output table 2>/dev/null || echo "Stack não encontrado"
          
          # Verificar permissões
          echo "3. Testando permissões S3:"
          aws s3api put-object \
            --bucket ${{ steps.bucket.outputs.bucket_name }} \
            --key test-permissions.txt \
            --body /dev/null \
            --content-length 0 2>&1 || echo "Sem permissão para escrever no bucket"

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: deployment-logs
          path: |
            deploy.log
            packaged.yaml
            .aws-sam/build/template.yaml
          retention-days: 7